package ui;

import java.util.ArrayList;
import java.util.List;

public class Deitel_CollectionTest_709 {

	public static void main(String[] args) {

		// Creëert een array colors met 4 kleuren.
		String[] colors = { "MAGENTA", "RED", "WHITE", "BLUE", "CYAN" };

		// Creëert een lege dynamische lijst.
		List<String> list = new ArrayList<>();

		for (String color : colors) {

			list.add(color);

		}

		// Creëert een gevulde array en een lege lijst.
		String[] removeColors = { "RED", "WHITE", "BLUE" };
		List<String> removeList = new ArrayList<>();

		// Plaatst in de lijst removeList alle objecten van colors.
		for (String color : removeColors) {
			removeList.add(color);
		}

	}
}
