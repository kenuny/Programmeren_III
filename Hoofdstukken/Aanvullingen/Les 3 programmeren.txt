Les 3 programmeren:
OOPIII_Col_Start gedaan

- set laat geen duplicaten toe
- HashSet = geen sortering, werkt volgens de hashcode
- TreeSet = wel gesorteerd

hernemen comparable en comparetor

Stack wordt niet meer gebruikt --> nu: arrayDeque omdat het performanter is.

deque 
enqueue 

met offer and poll opzoeken wat het overeenkomt met deque en enqueue

Map is een geassocieerde array, heeft een key en value (K, V)
wat is /'depricated'/

Er zijn drie views om naar een map te kijken (BELANGRIJK)
enkel geintresseerd in key --> keySet() verzameling van alle sleutels (unieke waarden) 
vb: map1.keySet();

enkel geintresseerd in waarden--> values() verzameling van alle waarden(niet unieke waarden) 
vb: map1.values();

geïntreseerd in de paren -> entrySet()

Kleine oefeningen oplossen tegen volgende les

Volgende les start hoofdstuk 17

Interface collection
bulkoperatie -> zijn operaties die op de gehele verzameling gaat inwerken.

